/*#################################################################*/
/*###############            FÁCEIS                 ###############*/
/*#################################################################*/

/* F.01
 * Elabore um programa que receba um número do usuário e informe 
 * se é par ou ímpar.  */
 
 void par_ou_impar(int num); 
 
 /* F.02
 * Determinar o valor máximo de dois números A e B dados como 
 * entrada. */
 
 int valor_maximo(int a, int b);

/* F.03
 * Dados dois números naturais como entrada, determinar o resto da 
 * divisão do maior pelo menor quando possível. */
 
 int resto_divisao(int n1, int n2);
 
 /* F.04
 * Faça um algoritmo que receba dois números inteiros e verifique se há 
 * a seguinte situação se o n1 está entre 50 e 200 e n2 está entre -1 e 9.
 * A saída deve ser 1 seos números satisfazem a situação e 0 
 * se não satisfazem. */
 
 int satisfaz_situacao(int n1, int n2);
 
 
 
 /*#################################################################*/
/*###############            MÉDIAS                 ###############*/
/*#################################################################*/
 
 /* M.01
 *Faça um algoritmo que determina o IMC (índice da massa corpórea) de uma 
 *pessoa e indique se ela esta magra, normal ou obesa. O IMC é calculado com o 
 *peso em kg, dividido pelo quadrado da altura, em metros, IMC = Peso/Altura2. Se o
 *IMC é menor que 20 estamos magros. Se o IMC está acima de 25 estamos 
 *obesos. Se o IMC estiver entre 20 e 25 estamos normais.*/

 void IMC(int peso, int altura);

/* M.02
 * Escreva um programa que lê três números correspondentes aos comprimentos 
 * dos lados de um triângulo e decide (imprime) se o triângulo é equilátero,
 * isósceles ou escaleno. */

 void tipo_triangulo(int l1, int l2, int l3);
 
/*#################################################################*/
/*###############            DIFÍCEIS               ###############*/
/*#################################################################*/

/* D.01
 * Receber 3 números inteiros como entrada e reescrevê-los em ordem 
 * crescente. */
 
 void ordem_crescente(int n1, int n2, int n3);
