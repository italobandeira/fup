//resolva a lista utilizando os recursos do objeto string do c++


//retornar o tamanho de uma string
int tamanho_str(string s);

//verificar se a string B eh substring da string a
int subs(string a, string b);

//copiar o conteudo da string b para string a
int copia(string a, string b);

//verificar se duas strings sao iguais
int iguais(string a, string b);

//retornar 1 se a string B for maior que A e zero caso contrario
int maior(string a, string b);

//retornar -1 se a string A for maior que B e zero caso contrario
int menor(string a, string b);

//retornar a comparacao, 0 se for iguais, 1 se B for maior, -1 se a for maior
int cmp(string a, string b);

//transformar a string str toda para uppercase
void to_upper(string a);
